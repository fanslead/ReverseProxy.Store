import filter from './filter'
import right from './right'
import router from './router'
import common from './common'
import myWindow from './window'
export default {
  mixins: [filter, right, router, common, myWindow]
}
